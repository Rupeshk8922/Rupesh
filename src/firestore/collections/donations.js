const donationStructure = {
  donorId: 'string', // User ID of the donor
  amount: 'number', // Amount of the donation
  date: 'timestamp', // Date and time of the donation
  eventId: 'string', // Optional: ID of the event the donation is associated with
  causeId: 'string', // Optional: ID of the cause the donation is associated with
};

export default donationStructure;