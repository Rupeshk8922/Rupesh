const useOrgId = () => {
  // This is a dummy hook that returns a hardcoded organization ID.
  // In a real application, this hook would fetch the organization ID
  // from authentication context, user data, or other sources.
  return "dummy-org-id-123";
};

export default useOrgId;