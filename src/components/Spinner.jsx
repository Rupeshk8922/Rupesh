import React from 'react';

const Spinner = () => {
  return (
    <div className="spinner-container"> {/* Optional: Add a container for styling */}
      Loading...
    </div>
  );
};

export default Spinner;